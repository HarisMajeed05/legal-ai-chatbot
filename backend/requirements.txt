# --- API framework ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-dotenv==1.0.1
pydantic==2.7.4
pydantic-settings==2.3.4
email-validator==2.2.0

# --- LLM orchestration (RAG) ---
langchain==0.2.6
langchain-community==0.2.6
langchain-groq==0.1.5
langchain-huggingface==0.0.3
langchain-text-splitters==0.2.2

# --- Vector store ---
faiss-cpu==1.8.0

# --- Embeddings (open source, runs on CPU, no API cost) ---
sentence-transformers==3.0.1

# --- Database ---
motor==3.4.0
pymongo==4.7.3

# --- Auth (for profile/project isolation) ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# --- Utilities ---
pypdf==4.2.0
reportlab==4.2.0
python-multipart==0.0.9
# Optional, only needed for OCR on scanned PDFs. Also requires the Tesseract
# OCR engine and Poppler system binaries, installed separately, see README.
pytesseract==0.3.10
pdf2image==1.17.0
